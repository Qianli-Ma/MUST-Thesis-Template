\ifwriteqEn
    \RequirePackage{fmtcount}
    % 转换英文的数值 one,two,...
    \renewcommand{\thechapter}{\Numberstring{chapter}}
    \renewcommand{\thesection}{\arabic{chapter}.\arabic{section}}
\else
    \RequirePackage{zhnumber}
    \renewcommand{\thechapter}{\zhnum{chapter}}
    \renewcommand{\thesection}{\zhnum{section}}
    \renewcommand{\thesubsection}{\zhnum{subsection}}
    \renewcommand{\thesubsubsection}{\arabic{subsubsection}}
\fi

\RequirePackage{titlesec}
% 设置标题深度,章,节,小节,小小节
\setcounter{secnumdepth}{4}

\ifwriteqEn
    \titleformat{\chapter}[hang]
        {\centering\bfseries\fontsize{18pt}{1.8\baselineskip}\selectfont}
        {Chapter \thechapter}
        {1 em}
        {\thispagestyle{fancy}}
        {}
    \titlespacing{\chapter}{0pt}{12pt}{12pt}[0 pt]
    % 2
    \titleformat{\section}[hang]
        {\centering\bfseries\fontsize{16pt}{1.8\baselineskip}\selectfont}
        {\thesection}
        {0.5 em}
        {}
    \titlespacing{\section}{0pt}{10pt}{10pt}[0 pt]
    %3
    \titleformat{\subsection}[hang]
        {\itshape\fontsize{14pt}{1.8\baselineskip}\selectfont}
        {\normalfont\thesubsection}
        {0.5em}
        {}

    \titlespacing{\subsection}{0pt}{5pt}{5pt}[0pt]

    % 4
    \titleformat{\subsubsection}[hang]
        {\itshape\fontsize{14pt}{1.8\baselineskip}\selectfont}
        {\normalfont\thesubsubsection}
        {1em}
        {}

\titlespacing{\subsubsection}{0pt}{5pt}{5pt}[0pt]

\else
    \titleformat{\chapter}[hang]
        {\centering\bfseries\fontsize{22pt}{\baselineskip}\selectfont}
        % {\centering\fontsize{22pt}{\baselineskip}\selectfont}
        {第\thechapter 章}
        {22pt}
        {\thispagestyle{fancy}}
        {}
    \titlespacing{\chapter}{0 mm}{-0.4cm}{0.2 cm}[0mm]
    % 2
    \titleformat{\section}[hang]
        {\bfseries\centering\fontsize{18pt}{\baselineskip}\selectfont}
        {第\thesection 節}
        {10pt}
        {}
    \titlespacing{\section}{0pt}{5pt}{5pt}[0 pt]
    %3
    \titleformat{\subsection}[hang]
        {\bfseries\fontsize{16pt}{\baselineskip}\selectfont}
        % {\fontsize{16pt}{\baselineskip}\selectfont}
        {\thesubsection 、}
        {0 em}
        {}
    \titlespacing{\subsection}{0pt}{5pt}{5pt}[0 pt]
    % 4
    \titleformat{\subsubsection}[hang]
        {\bfseries\fontsize{14pt}{\baselineskip}\selectfont}
        % {\fontsize{14pt}{\baselineskip}\selectfont}
        {\thesubsubsection.}
        {0.2 em}
        {}
    \titlespacing{\subsubsection}{0pt}{5pt}{5pt}[0 pt]
\fi



\RequirePackage{caption}
\RequirePackage{subcaption}
\captionsetup[table]{position=above}
\captionsetup{
    aboveskip = 0.2 em, % 標題上下文距離
    belowskip = 0.2 em,
    labelsep  = quad, % 設置標題分割符
    labelfont = bf,   % 設置標題樣式
    font = large,
    justification = centering, % 标题居中
}

% 修改图表前缀
\ifwriteqEn
    \renewcommand{\figurename}{Figure}
    \renewcommand{\tablename}{Table}
    \renewcommand{\algorithmcfname}{Alg}
\else
    \renewcommand{\figurename}{圖}
    \renewcommand{\tablename}{表}
    \renewcommand{\algorithmcfname}{算法}
\fi

% 修改图表序号
\renewcommand\thefigure{\arabic{chapter}-\arabic{figure}}
\renewcommand\thetable{\arabic{chapter}-\arabic{table}}
\renewcommand\thealgocf{\arabic{chapter}-\arabic{algocf}}
\renewcommand\theequation{\arabic{chapter}.\arabic{equation}}











